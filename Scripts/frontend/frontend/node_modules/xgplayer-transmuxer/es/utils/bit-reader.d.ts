import { ByteReader } from './byte-reader';
export declare class BitReader {
    private val;
    size: number;
    offset: number;
    constructor(val: number, size: number);
    static fromByte(byte: ByteReader, len: number): BitReader;
    skip(len: number): void;
    read(len: number): number;
}
